# Robot-No.-3-is-practical.-ZMI-14-Octb


````markdown
# ⚙️ Практична робота 3: Реалізація системи Нечіткої Логіки (Fuzzy Logic)

## Тема: Автоматизоване керування краном гарячої води

| Університет: | МІЖНАРОДНИЙ НАУКОВО-ТЕХНІЧНИЙ УНІВЕРСИТЕТ ІМЕНІ АКАДЕМІКА ЮРІЯ БУГАЯ |
| :--- | :--- |
| **Дисципліна:** | Інтелектуальний аналіз даних |
| **Спеціальність:** | Інформаційні та комунікаційні технології – «Бакалавр» |
| **Група:** | І-23 (4-й курс) |
| **Виконали:** | Кириченко Кіріл Юрійович, Сидоров Нікіта Геннадійович |
| **Перевірив:** | Гриб'юк Олена Олександрівна |
| **Дата:** | Київ 2025 |

---

## 🚀 1. Ініціалізація та Визначення Змінних

Цей блок імпортує необхідні бібліотеки (`numpy`, `skfuzzy`) та визначає **Універсуми Дискурсу** для всіх вхідних (антецеденти) та вихідної (консеквент) змінних.

### 🐍 Код
```python
import numpy as np
import skfuzzy as fuzz
from skfuzzy import control as ctrl

# --- Вхідні змінні (Антецеденти) ---
temp = ctrl.Antecedent(np.arange(10, 81, 1), 'temp')    # Температура води [10; 80]
head = ctrl.Antecedent(np.arange(0, 1.01, 0.01), 'head') # Напір води [0.0; 1.0]

# --- Вихідна змінна (Консеквент) ---
valve = ctrl.Consequent(np.arange(-10, 10.1, 0.1), 'valve') # Кут повороту крана [-10; 10]
````

### 📝 Пояснення

Вихідна змінна `valve` інтерпретується так:

  * **Негативні значення ($\approx -10$):** Відкрити кран гарячої води (збільшити потік).
  * **Позитивні значення ($\approx 10$):** Закрити кран гарячої води (зменшити потік).

-----

## ⚙️ 2. Визначення Функцій Належності (Терм-множини)

Цей етап визначає лінгвістичні терми та відповідні їм **трикутні функції належності** (`fuzz.trimf`) для перетворення чітких чисел у нечіткі множини.

### 🐍 Код

```python
# --- Функції належності для temp ---
temp['cold'] = fuzz.trimf(temp.universe, [10, 20, 35])
temp['mid'] = fuzz.trimf(temp.universe, [30, 35, 40])
temp['hot'] = fuzz.trimf(temp.universe, [40, 50, 80])

# --- Функції належності для head ---
head['small'] = fuzz.trimf(head.universe, [0, 0.1, 0.3])
head['norm'] = fuzz.trimf(head.universe, [0.25, 0.5, 0.75])
head['big'] = fuzz.trimf(head.universe, [0.6, 0.8, 1])

# --- Функції належності для valve (Дія) ---
valve['open_q'] = fuzz.trimf(valve.universe, [-10, -7, -5]) # Швидко відкрити
valve['open_s'] = fuzz.trimf(valve.universe, [-6, -3, -1])  # Повільно відкрити
valve['norm'] = fuzz.trimf(valve.universe, [-2, 0, 2])     # Залишити як є
valve['close_s'] = fuzz.trimf(valve.universe, [1, 3, 6])    # Повільно закрити
valve['close_q'] = fuzz.trimf(valve.universe, [5, 7, 10])   # Швидко закрити
```

### 🖼️ Візуалізація Функцій Належності

  * 
  * 
  * 
> **Аналіз:** Завдяки перетину функцій (наприклад, між `cold` і `mid` на 35°C) система може одночасно активувати кілька правил, що є основою плавного керування.

-----

## 🧠 3. Створення Правил та Системи Керування

Цей блок містить **Базу Правил** (логіку системи) у форматі **ЯКЩО (умова) ТО (дія)** та ініціалізує виконавчий механізм (симулятор).

### 🐍 Код (Фрагмент)

```python
# --- Створення правил (База правил) ---
rule1 = ctrl.Rule(temp['cold'] & head['small'], valve['open_q']) # Швидко відкрити
rule4 = ctrl.Rule(temp['hot'] & head['big'], valve['close_q'])   # Швидко закрити
rule3 = ctrl.Rule(temp['mid'] & head['norm'], valve['norm'])     # Залишити як є

# --- Створення контролера ---
valve_ctrl = ctrl.ControlSystem([rule1, rule2, rule3, rule4, rule5])
valve_sim = ctrl.ControlSystemSimulation(valve_ctrl)
```

### 📝 Ключові Правила

| № | Умова (IF) | Дія (THEN) |
| :---: | :--- | :--- |
| **1** | Температура **холодна** І Напір **малий** | **Швидко відкрити** (`open_q`) |
| **3** | Температура **помірна** І Напір **нормальний** | **Залишити як є** (`norm`) |
| **4** | Температура **гаряча** І Напір **великий** | **Швидко закрити** (`close_q`) |

-----

## 🧪 4. Тестування та Висновок (Дефазифікація)

Система була протестована з конкретними числовими вхідними даними, щоб продемонструвати повний цикл нечіткого висновку.

### ⚙️ Тестовий Вхід

| Змінна | Вхідне значення |
| :---: | :---: |
| Температура (`temp`) | **45.0 °C** |
| Напір (`head`) | **0.4** |

### 🐍 Код

```python
valve_sim.input['temp'] = 45  
valve_sim.input['head'] = 0.4 
valve_sim.compute()

# Вивід результату
print(f"Кут повороту крана: {valve_sim.output['valve']:.2f}")

# Візуалізація агрегації та дефазифікації
valve.view(sim=valve_sim)
```

### 🎯 Результат Обчислення

```
Кут повороту крана: 3.01
```

### 📊 Візуалізація Висновку (Агрегація та Дефазифікація)

> **Аналіз Figure 4:**
>
> 1.  **Агрегація (Червона заштрихована область):** Це об'єднана нечітка множина, сформована активацією правил. Вона знаходиться переважно в зоні термів `close_s` (повільно закрити) та `close_q` (швидко закрити).
> 2.  **Дефазифікація (Чорна вертикальна лінія):** Фінальний чіткий висновок ($\mathbf{3.01}$) обчислюється як Центр Тяжіння (CoG) агрегованої області.
> 3.  **Інтерпретація:** Оскільки 45°C є гарячою водою, система видала позитивний кут повороту ($+3.01$), що означає **повільне закриття крана гарячої води** для зниження температури. Це підтверджує, що логіка керування працює правильно.

-----

## 🌟 Висновок до Роботи

Розроблена система успішно демонструє застосування **Нечіткої Логіки (Fuzzy Logic)** для вирішення задачі автоматичного керування температурою води.

### Ключові Досягнення

  * **Успішна Реалізація:** Створено повноцінну нечітку систему (контролер Mamdani) за допомогою бібліотеки `scikit-fuzzy`.
  * **Адекватність:** База з п'яти правил успішно моделює інтуїтивну логіку людини-оператора, забезпечуючи плавні коригуючі дії.
  * **Практична Цінність:** На відміну від класичної бінарної логіки, нечітка система забезпечує **плавний перехід між станами** ("повільно закрити" замість "вимкнути"), що є критично важливим для стабільного та комфортного регулювання.

<!-- end list -->

```
```
